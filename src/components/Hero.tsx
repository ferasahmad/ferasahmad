import { NextPage } from "next";
import SectionContainer from "./SectionContainer";

const Hero: NextPage = () => {
  return (
    <SectionContainer>
      <h1 className="text-center text-7xl tracking-[10px] lg:text-[70px] lg:text-nowrap sm:text-[20px] font-bold">
        FERAS AHMAD
      </h1>
    </SectionContainer>
  );
};

export default Hero;
